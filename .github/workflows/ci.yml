name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: ruff check .
      - run: pytest
      - name: Run example benchmark
        run: agent-reliability examples/cases.jsonl examples/traces.jsonl --fail-under 1.0
      - name: Verify fault detection and regression gate
        run: |
          agent-reliability-inject examples/traces.jsonl artifacts/faulty-traces.jsonl --fault drop_evidence
          if agent-reliability-compare examples/cases.jsonl examples/traces.jsonl artifacts/faulty-traces.jsonl; then
            echo "Expected the regression gate to reject fault-injected traces"
            exit 1
          fi
      - uses: actions/upload-artifact@v4
        if: matrix.python-version == '3.12'
        with:
          name: reliability-report
          path: artifacts/
